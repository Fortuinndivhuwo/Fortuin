#We start by creating define functionas

This calculator is made to do four functions
which are addiction,subtraction,multiply and divide

#Create a while loop
In this loop we make the condition be a boolean True
and it can only be broken by the user if they want to quit.

In the loop we create a list of the function i mentioned above
and print it individually with numbers to represent their position
we print them by a for loop.

#Get the user to choice and use the functions with the number he/she
what's to calculate.

This is still i the while loop.
We ask user to enter number function or "q" to quit.
Then ask them to also enter the two numbers to calculate.
#preventing errors
While dealing with these numbers we also make sure
that the use doesn't divide by zero since its an error in maths.
so we check if the second number isn't a zero.
we also check if the user doesn't enter something other than 1/2/3/4 or "q"

